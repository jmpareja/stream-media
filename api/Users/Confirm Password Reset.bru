meta {
  name: Confirm Password Reset
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/users/password-reset/confirm
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "token": "{{resetToken}}",
    "new_password": "newpassword123"
  }
}
